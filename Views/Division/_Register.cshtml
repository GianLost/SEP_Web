@model Division
@using SEP_Web.Models
@using SEP_Web.Models.StructuresModels
@using SEP_Web.Services
@inject IInstituitionServices _instituitionServices
@{
    ViewData["Title"] = "Cadastrar Órgão";
}

<!-- Partial View de Cadastro de Órgãos -->

<div>

    <form id="division-form">

        <div class="form-group p-4">
            <label for="division-instituitionid" hidden>Órgão: </label>
            <select class="form-select" name="instituitionId" id="division-instituitionid" asp-for="InstituitionId">
                <option value="" selected> -- selecione o órgão -- </option>
                @foreach (var i in await _instituitionServices.InstituitionsList())
                {
                    <option value="@i.Id">@i.Name</option>
                }
            </select>
            <div class="validate-fields mb-1">
                <span class="select-validation-error"></span>
            </div>

            <label class="form-label" for="division-name" hidden>Nome da divisão: </label>
            <input type="text" placeholder="Informe a descrição da divisão :" class="form-control" id="division-name" name="name" autocomplete="disabled"/>
            <div class="validate-fields">
                <span class="field-validation-error"></span>
            </div>

        </div>

        <div class="d-flex justify-content-start modal-footer p-2">

            <button type="submit" class="btn btn-sm btn-primary" title="Cadastrar ..."><i class="bi bi-check-circle-fill" title="Cadastrar ..."></i></button>
            <a asp-controller="Division" asp-action="Index" class="btn btn-sm btn-danger" title="Cancelar ..."><i class="bi bi-x-circle-fill" title="Cancelar ..."></i></a>

        </div>

    </form>

</div>

<!-- Partial View de Cadastro de Órgãos -->